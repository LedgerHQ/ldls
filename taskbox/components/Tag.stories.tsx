import '../global.css';
import { Linking, Text, View } from 'react-native';
import { Tag } from './Tag';

const meta = {
  title: 'Welcome',
  component: Tag,
};

export default meta;

export const GettingStarted = {
  args: {},
  parameters: {
    noBackground: true,
  },
};
