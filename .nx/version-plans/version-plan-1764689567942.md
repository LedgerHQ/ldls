---
'@ledgerhq/ldls-ui-react': patch
---

- fix(DialogHeader): keep close button aligned to right when no back button is present
- Fix(Dialog): fix gradient overlays (InfoStateError and InfoStateSuccess) to ensure close button remains clickable
