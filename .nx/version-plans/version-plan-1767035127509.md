---
'@ledgerhq/lumen-ui-rnative': patch
---

- refactor(ui-rnative): rename `LumenStyleSheet.useCreate()` to `useStyleSheet()`
- refactor(ui-rnative): rename `LumenStyleSheet.useTheme()` to `useTheme()`
- refactor(ui-rnative): remove `mergeStyles` utility and use `StyleSheet.flatten()` instead
- refactor(ui-rnative): restructure and simplify the `/styles` folder
- refactor(ui-rnative): makes the useStyleSheet deps array required for an explicit API